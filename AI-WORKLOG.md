# AI Work Log

This log records how AI tools were used throughout the development of Product Content Studio, per the task requirements and the logging policy in [AGENTS.md](AGENTS.md#logging-to-ai-worklogmd).

## Tools & models used

| Tool                         | Model(s)                     | Role                                                                                       |
| ---------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------ |
| Claude Code                  | Sonnet 5 (`claude-sonnet-5`) | Primary AI pair-programmer: planning, scaffolding, implementation, tests, docs             |
| Figma MCP (via Claude Code)  | —                             | UI design directly on the user's Figma account for the Design Tools bonus — see note below |

No other AI coding tools were used on this project (no Cursor/Copilot autocompletion, etc.) — Claude Code was the sole AI pair-programmer throughout.

> Originally planned to use Claude.ai's separate "Claude Design" canvas feature (see Prompt 1 in PROMPTS-HISTORY.txt), but the user connected a Figma MCP server to this Claude Code session instead (Prompt 17), which let Claude Code draw directly into the Figma file via the Plugin API — no separate Claude.ai session or quota involved. See the 2026-09-17 entries below.

## How to read this log

Entries are appended chronologically (oldest first) as work happens — see [AGENTS.md](AGENTS.md) for the logging policy. Each entry follows this template:

```
### YYYY-MM-DD — <short title>

- **Task:** what was being worked on
- **AI contribution:** what the AI tool produced/suggested
- **My contribution:** what I decided, changed, reviewed, or wrote myself
- **Verification:** how it was checked (tests run, manual check, code review)
- **Reference:** commit hash / file / PR link (if applicable)
```

Notable AI-code decisions (the 2–3 examples required by the task) are marked with **⭐ Notable decision** so they're easy to find.

## Role of automated tests in verifying AI-generated code

All code in this repository — application and tests alike — was written by Claude Code and reviewed/directed by the candidate; every "Verification" line in the entries below is how each change was actually checked, not just claimed.

### How automated tests verified AI-generated code

Every phase's code was verified against real infrastructure, not just by reading it or trusting green test output: a real Docker Postgres for manual testing and the backend integration suite (via `testcontainers`), a real browser (screenshots, network logs, DOM reads) for UI/auth flows, and a real `next dev` server for the Cypress e2e suite. Several fixes (font loading, the transparent-refresh flow, the `/admin/login` redirect bug, the two bugs below) were found this way, not by code review.

The automated tests themselves caught two real bugs that inspection had missed, which is itself evidence they're doing real work rather than padding a coverage number: `LogoutButton`'s missing `catch` (an unhandled promise rejection on a failed logout, caught by a frontend integration test) and `GET /products/[slug]` returning HTTP 200 instead of 404 for a draft/unknown slug (an App Router streaming quirk, caught only by the Cypress e2e suite — the backend integration tests call Route Handlers directly with no live server, so they structurally couldn't have seen it). Both are detailed in their own entries below.

### Quality assessment of the AI-written tests themselves

The AI-written test assertions were quality-checked, not just trusted because they passed: they were written (and reviewed) to check actual persisted state, not just response shape — e.g. the save-validation integration tests re-read the row via Prisma after a rejected `PATCH` to confirm it's genuinely unchanged, rather than only checking the 400 status; the frontend save-flow tests assert the input's DOM value is preserved after a rejected save, not just that an error message appeared.

One bug in the tests themselves was caught this way too: an integration test asserted `toBeInstanceOf(Array)` on a JSON response body and failed even though the underlying behavior was correct, because Next's `Response`/JSON implementation crosses a realm boundary and the array isn't an instance of the test file's own `Array` constructor. It was fixed to `Array.isArray(...)` once the actual cause was understood, rather than loosened until it happened to pass — a concrete example of the AI-written tests being debugged and corrected, not just generated and left as-is.

---

## Entries

### 2026-09-14 to 2026-09-16 — Planning: stack, architecture, and development order

- **Task:** Kick off the project by reading PROJECT-REQUIREMENTS.md, choosing technologies from the options offered, and defining strategic decisions (architecture principles, test strategy, bonus scope/order) before any code is written.
- **AI contribution:** Claude Code read the requirements, proposed a set of clarifying questions on ambiguous/bonus-scope points (bonus time budget & priority order, test-DB strategy, LLM provider for the bonus, Design Tools bonus approach), and drafted the initial [AGENTS.md](AGENTS.md), this AI-WORKLOG.md, and the README.md skeleton based on the answers.
- **My contribution:** Chose the core stack (Next.js, Prisma, Zod, PostgreSQL, JWT, npm, shadcn/ui, Prettier+ESLint, Jest), answered the clarifying questions (bonus tasks get their own ~2–3h budget separate from the 6–8h core budget; testcontainers over pg-mem/SQLite for test DB fidelity; OpenAI for the bonus LLM integration; Figma design via Claude Design for the Design Tools bonus), and approved the resulting plan.
- **Verification:** N/A (planning only, no code yet).
- **Reference:** [AGENTS.md](AGENTS.md)

### 2026-09-16 — Phase 1: scaffolding

- **Task:** Execute Phase 1 of [DEVELOPMENT-PLAN.md](DEVELOPMENT-PLAN.md): Next.js + TS project setup, Prisma schema, Docker Compose (Postgres), env config, shared Zod schemas/types, layered folder structure, and placeholder pages for every screen.
- **AI contribution:** Claude Code scaffolded the Next.js app (`create-next-app`) and shadcn/ui, wrote `prisma/schema.prisma` (`AdminUser`, `RefreshToken`, `Product`), `docker-compose.yml`, `.env.example`, the Zod validation schema for the editable product fields, the domain `Product` type, the Prisma client singleton, and all five placeholder route pages. It also diagnosed and fixed two environment issues (see notable decisions below) and verified the result by running `npm run build`, `npm run lint`, and a live `next dev` session with screenshots of all five routes.
- **My contribution:** Reviewed each generated file, decided to move `shadcn` from `dependencies` to `devDependencies` (it's a CLI, not a runtime dep), decided to keep only `.claude/skills` from Prisma's auto-generated skill files and drop the duplicate `.windsurf`/`.agents` copies (unused tools, pure clutter), and approved the driver-adapter and Node-version fixes below after seeing the reasoning.
- **Verification:** `npm run build` (typecheck + production build succeeds, all 5 routes present), `npm run lint` (no errors), and a manual pass through all 5 placeholder routes in a live `next dev` server with screenshots confirming each renders its expected placeholder content.
- **Reference:** [prisma/schema.prisma](prisma/schema.prisma), [docker-compose.yml](docker-compose.yml), [src/server/db/client.ts](src/server/db/client.ts)

**⭐ Notable decision 1 — Node version bump (20.14 → 22.21.1).** `npm install` kept failing with an unrelated-looking arborist crash (`Cannot read properties of null (reading 'edgesOut')`) on the environment's default Node 20.14. Root cause turned out to be that Prisma 7 requires Node `^20.19 || ^22.12 || >=24`, and the resolver choked before it could even report a clean engine error. Claude Code found Node 22.21.1 already installed via `nvm`, switched the project to it, and pinned it in `.nvmrc` and the README so the failure won't resurface for a reviewer running an older default Node. Verified by a clean `rm -rf node_modules package-lock.json && npm install` succeeding under 22.21.1.

**⭐ Notable decision 2 — Prisma 7 requires an explicit driver adapter.** After fixing the Node version, `npm install prisma @prisma/client` initially resolved `@prisma/client@8.0.0-rc.15` (a release-candidate pulled in because `prisma`'s dependency range was left unpinned) while `prisma` resolved to the stable `7.10.0` — a version mismatch between CLI and client. Claude Code pinned both to the same stable `7.10.0`. Separately, `next build`'s typecheck then failed because Prisma 7's generated client no longer accepts a bare `datasourceUrl` option — it requires an explicit driver adapter. Claude Code installed `@prisma/adapter-pg` + `pg` and wired `new PrismaPg({ connectionString: process.env.DATABASE_URL })` into the `PrismaClient` constructor in [src/server/db/client.ts](src/server/db/client.ts). Verified by `npx prisma migrate dev` applying successfully against the Docker Postgres instance and `npm run build` passing cleanly afterward.

**⭐ Notable decision 3 — Repo hygiene: pruning Prisma's auto-generated AI skill files.** `prisma init` (v7) auto-installed identical copies of its CLI/client-API skill docs into `.claude/skills/`, `.windsurf/skills/`, and `.agents/skills/`, plus a `skills-lock.json`. Since this project only uses Claude Code, keeping three duplicate copies was pure repo clutter with no benefit. Candidate decision: delete `.windsurf/` and `.agents/`, keep `.claude/skills/` (useful — it's real skill content for the tool actually in use) and `skills-lock.json` (needed for Prisma to track/update it). No functional verification needed; confirmed `prisma generate`/`migrate` still worked afterward.

### 2026-09-16 — Phase 2: backend core

- **Task:** Execute Phase 2 of [DEVELOPMENT-PLAN.md](DEVELOPMENT-PLAN.md): JWT access+refresh auth (login/logout, protected admin routes, transparent refresh), a seed script (test admin + 3 demo products), the admin product API (read/update only), and the public product API (published-only, drafts unreachable), all server-side Zod-validated.
- **AI contribution:** Claude Code wrote the full auth stack (`src/server/auth/{passwords,tokens,cookies,guard}.ts`), repositories and services (`src/server/repositories/*`, `src/server/services/*`), all Route Handlers under `src/app/api/`, the Edge proxy (`src/proxy.ts`) that gates `/admin/*` pages, and `prisma/seed.ts`. It also caught and fixed a Next.js 16 deprecation warning during `npm run build` (`middleware` → `proxy` file convention) by reading `node_modules/next/dist/docs/.../proxy.md` and renaming the file/export accordingly, rather than leaving a deprecation warning in the build output.
- **My contribution:** Reviewed the access+refresh token design end-to-end (asked for and got an explanation of why refresh tokens are DB-backed — see the "why do we store refresh tokens" discussion), approved the `jose`-over-`jsonwebtoken` choice below, and manually exercised every endpoint (see verification) rather than trusting the code review alone.
- **Verification:** `npm run build`/`lint`/`tsc --noEmit` all clean. Then, against the real Docker Postgres instance with seeded data, manually exercised the full flow with `curl`: public catalog lists only published products; a draft's public detail route 404s (both by slug and — implicitly — by not appearing in the catalog); admin routes 401 without auth; login with a wrong password 401s; login succeeds and sets cookies; admin list/detail/update work once authenticated; an invalid update (description over 1000 chars) 400s and does not persist; a valid update flips a product from draft to published and it then appears in the public catalog; refresh-token rotation issues a new access token and immediately invalidates the previous refresh token; logout revokes the refresh token so a further refresh 401s; and `/admin/products` redirects to `/admin/login` (307) when unauthenticated while `/admin/login` itself stays reachable.
- **Reference:** [src/server/services/auth-service.ts](src/server/services/auth-service.ts), [src/proxy.ts](src/proxy.ts), [prisma/seed.ts](prisma/seed.ts)

**⭐ Notable decision 4 — `jose` instead of `jsonwebtoken`, and no DB access from the Edge proxy.** The access-token check that gates `/admin/*` pages needs to run in Next's Proxy/Middleware layer, which executes on the Edge runtime by default — a constrained environment without Node's `net`/TCP sockets, so a real DB-backed refresh-token rotation can't happen there. Claude Code chose `jose` (Web Crypto based, Edge-compatible) over the more commonly-known `jsonwebtoken` (Node-only) for all JWT signing/verification, and split the design so the proxy only ever does a stateless signature/expiry check and redirects to `/admin/login` on failure; the DB-backed refresh-token rotation lives entirely in `POST /api/admin/auth/refresh`, a Node-runtime Route Handler, called by the admin client whenever an API request 401s. This avoids forcing the whole app onto a Node-runtime middleware config just to keep one code path. Verified by the `curl` checks above (redirect behavior and refresh rotation both confirmed working).

### 2026-09-17 — Phase 3: Figma design (Admin Login, Product List, Product Editor)

- **Task:** Design Tools bonus + Phase 3 of DEVELOPMENT-PLAN.md. Produce Figma mockups (desktop + mobile, main states) for the admin screens, then use them as the basis for the real Phase 4 UI implementation.
- **AI contribution:** Claude Code used a Figma MCP server the user connected to this session to create a new Figma file and draw every screen directly onto the user's account via the Plugin API (`use_figma`), writing the JavaScript for each layout itself (colors, spacing, typography, component structure) rather than using a design-generation black box. Produced: Admin Login (desktop + mobile, default + invalid-credentials error state), Admin Product List (desktop + mobile, default state, using the real seeded product names/statuses), and Admin Product Editor (desktop + mobile, default state, using the real "Wireless Mouse M1" seed data including its actual characteristics/description/SEO fields). Iterated through several Figma Plugin API errors (auto-layout `FILL` sizing set before a node was parented; CSS-selector queries breaking on node names containing spaces or em dashes) — all self-diagnosed and fixed via the tool's own error messages, verified with `node.screenshot()` after each build.
- **My contribution:** Chose to draw directly in Figma via this session instead of the separately-billed Claude.ai "Design" canvas feature, once Figma MCP access was available (see PROMPTS-HISTORY.txt, prompts 17–18) — a cost/workflow call, not a code correctness one. Deliberately scoped the pilot down to one screen with no extra states first ("I want to see some result in few minutes max") to validate quality before committing quota to the rest. Requested and got the desktop Product List rows narrowed and centered (640px, not full-bleed) after judging the first pass too wide. Chose Admin Login as the pilot screen and reasoned through screen priority order.
- **Verification:** Visual review of every `node.screenshot()` output before proceeding to the next screen/state. No code was written yet at this stage — verification was purely "does this look like a usable, on-brand mockup," which is why Phase 4 (below) re-verifies everything against the real running app.
- **Reference:** [Figma file](https://www.figma.com/design/XEWl5YinPePK2aQajd9xE3/Product-Content-Studio-%E2%80%94-UI-Design?node-id=0-1&t=u1ntu3s3m0f4qGVE-1) (7 artboards); PROMPTS-HISTORY.txt prompts 17–27.

**⭐ Notable decision 5 — Hit an external rate limit mid-task and changed plan rather than wait.** The Figma team's Starter-tier MCP tool-call limit was hit while building the mobile Admin Product Editor (the 7th/8th artboard), unrelated to Claude/Anthropic usage. Rather than wait for the reset or pay to upgrade, Claude Code pointed out that it already had the complete design spec in its own context (it wrote every color/spacing/text value itself) and could skip Figma's Dev Mode/`get_design_context` export path entirely, going straight to hand-written shadcn/ui components from the same spec. This turned out faster and more reliable than a design-to-code extraction would have been, since there was zero ambiguity about what each mockup contained.

### 2026-09-17 — Phase 4 (partial): admin UI implementation from the Figma spec

- **Task:** Implement real, working pages for the three screens designed in Figma — Admin Login (incl. error state), Admin Product List, Admin Product Editor — wired to the Phase 2 APIs, matching the mockups' visual spec.
- **AI contribution:** Claude Code added the needed shadcn/ui primitives (`input`, `textarea`, `label`, `card`, `badge`), then wrote: the login page as a client component posting to `/api/admin/auth/login` with inline error/loading states; the product list as a Server Component calling `productService.listForAdmin()` directly (no HTTP round-trip); the product editor as a Server Component (data fetch + read-only characteristics) plus a client `ProductEditorForm` (character counters, a Draft/Published toggle, client-side validation that disables Save on invalid input in addition to the existing server-side Zod check, and error handling that never clears the user's typed edits on a failed save). Also a shared `AdminTopBar`/`LogoutButton`/`StatusBadge`.
- **My contribution:** Asked a clarifying question about the actual difference between the admin list and the public catalog beyond draft visibility — which led directly to finding and flagging a real gap (see notable decision 6). Reviewed each component against the Figma screenshots before accepting it.
- **Verification:** `npm run build`/`lint`/`tsc --noEmit` clean. Then a full manual pass in a live `next dev` browser session: login with a wrong password shows the red-bordered error banner and does not clear the typed fields; login with the seeded admin credentials redirects to the product list; the list matches the seed data and links to the editor; editing the SEO title past 60 characters turns the counter red and disables Save (confirmed via `read_page`, the button is genuinely `disabled`, not just styled that way); a valid save shows "Saving…" then "Saved." and the new value was confirmed to have actually persisted via a direct `psql` query against the Docker Postgres instance; the mobile layout (375px viewport) was checked for all three pages and matches the Figma mobile mockups' stacking behavior. Also caught and fixed a real bug during this pass: `/admin/products` had no dynamic route segment and no `cookies()`/`headers()` call, so Next.js was statically prerendering it at build time — meaning it would have served a frozen, build-time snapshot of the product list to every admin forever. Fixed by adding `export const dynamic = "force-dynamic"` to both admin data pages.
- **Reference:** [src/app/admin/login/page.tsx](src/app/admin/login/page.tsx), [src/components/admin/product-editor-form.tsx](src/components/admin/product-editor-form.tsx), [src/app/admin/products/page.tsx](src/app/admin/products/page.tsx)

**⭐ Notable decision 6 — Found a real gap by questioning my own earlier work.** The user asked what actually differs between the admin product list and the public catalog beyond draft visibility. Answering that properly meant re-reading `product-repository.ts` from Phase 2, which revealed that `listPublished()` (used by the public catalog) selects `{ id, name, status }` — no `slug` — even though the public product page routes by slug (`/products/[slug]`), not `id`. The public catalog UI (not yet built) would have had no way to link to a product page at all. Flagged to the user rather than silently fixed, since it touches a public API contract. Fixed in the next entry below.

### 2026-09-17 — Fix: public/admin product list type contracts

- **Task:** Fix the gap from notable decision 6: `ProductRepository.listPublished()` returned `{ id, name, status }` (the admin shape) instead of exposing `slug`, which the public product page routes by. Also address the user's forward-looking concern that a two-type split (admin vs. public list item) would mean updating field lists in two disconnected places as the app grows.
- **AI contribution:** Claude Code proposed two options — add `slug` to the existing shared type, or split into `AdminProductListItem`/`PublicProductListItem` — recommended the split for the security/coupling reasons in AGENTS.md, then addressed the follow-up "won't this double future work?" concern by deriving both types via `Pick<Product, ...>` from the single canonical `Product` type in `lib/types/product.ts`, rather than declaring them as independent interfaces. Updated `product-repository.ts`'s Prisma `select`s and `product-service.ts`'s return types to match.
- **My contribution:** Chose the type-split approach over the simpler add-a-field option, and specifically pushed back on it with "we'll need more public fields later — how do we avoid double maintenance," which is what led to the `Pick`-derived design rather than two hand-duplicated interfaces.
- **Verification:** `npm run build`/`lint`/`tsc --noEmit` clean. Live-verified via `curl` against the real Docker Postgres instance: `GET /api/products` now returns exactly `{ slug, name }` per item (no `id`, no `status`); `GET /api/admin/products` (authenticated) is unchanged, still `{ id, name, status }`.
- **Reference:** [src/server/repositories/product-repository.ts](src/server/repositories/product-repository.ts), [src/server/services/product-service.ts](src/server/services/product-service.ts)

### 2026-09-17 — Fix: fonts didn't match the Figma design

- **Task:** The user tested the running app themselves (via a dev-server link) and reported the font didn't match the Figma mockups.
- **AI contribution:** Claude Code traced it to `src/app/globals.css`: `--font-sans: var(--font-sans)` is a self-referential, always-undefined CSS variable (an artifact of running `create-next-app`, which wired up `--font-geist-sans`/`--font-geist-mono`, and then `shadcn init` overwriting `globals.css` with a template that expects the project to supply its own `--font-sans` — nobody ever connected the two). Because the variable never resolved, the browser fell back to its raw UA default font (a serif), not even Geist. Fixed by loading `Inter` (the font used throughout every Figma mockup) via `next/font/google` bound to `--font-sans` in `src/app/layout.tsx`, and removed the now-dangling `--font-mono: var(--font-geist-mono)` reference in `globals.css` since Geist was dropped entirely.
- **My contribution:** Caught the visual mismatch by actually running the app instead of trusting the code; reported it as a font issue without diagnosing the root cause, which Claude Code then found and fixed.
- **Verification:** `npx tsc --noEmit`/`npm run lint`/`npm run build` clean. Visually re-verified in a live `next dev` session — the login and product list pages now render in Inter, matching the Figma screenshots, instead of the browser's default serif.
- **Reference:** [src/app/layout.tsx](src/app/layout.tsx), [src/app/globals.css](src/app/globals.css)

### 2026-09-17 — Fix: wire up the transparent refresh flow, add loading spinners

- **Task:** The user asked for an honest completeness assessment of the admin UI. Claude Code flagged that the access+refresh JWT design (Phase 2, AGENTS.md) was never actually wired into the client: a 401 on save just showed a raw error instead of silently refreshing and retrying, and there was no route-level loading UI. The user asked to fix both.
- **AI contribution:** Added `src/lib/authenticated-fetch.ts` — a `fetch` wrapper that retries once via `POST /api/admin/auth/refresh` on a 401, used by `ProductEditorForm`'s save call; on a 401 that survives the refresh attempt (refresh token itself dead), redirects to `/admin/login` instead of showing a confusing error. Added shadcn's `Spinner` component (`Loader2Icon` + `animate-spin`) to the login and save buttons' pending states, and `loading.tsx` route files for `/admin/products` and `/admin/products/[id]` for the page-navigation loading state.
- **My contribution:** Asked for the completeness review that surfaced the gap, then asked specifically to connect the refresh endpoint and add a shadcn spinner rather than a custom one.
- **Verification:** `npx tsc --noEmit`/`npm run lint`/`npm run build` clean. Verified the actual refresh flow end-to-end rather than just trusting the code: temporarily shortened the access-token TTL to 15 seconds in `tokens.ts`, logged in, waited past expiry while sitting on the editor page, then saved — confirmed via `read_network_requests` the exact sequence `PATCH → 401`, `POST /api/admin/auth/refresh → 200`, `PATCH → 200` (retried), the UI showed "Saved." with no visible error, and the new value was confirmed persisted via a direct `psql` query. TTL reverted to 15m and the test data reverted afterward.
- **Reference:** [src/lib/authenticated-fetch.ts](src/lib/authenticated-fetch.ts), [src/components/admin/product-editor-form.tsx](src/components/admin/product-editor-form.tsx), [src/components/ui/spinner.tsx](src/components/ui/spinner.tsx)

### 2026-09-17 — Strategic decision: axios for all client-side HTTP, replacing fetch

- **Task:** The user specified (after the fact) that all client-side HTTP calls should use axios, not native `fetch`, and asked for the tech stack to be updated and the existing client code refactored.
- **AI contribution:** Recorded the decision in [AGENTS.md](AGENTS.md)'s tech stack table. Added `src/lib/api-client.ts` (a shared axios instance with `withCredentials: true` and `validateStatus: () => true`, the latter so callers can inspect `response.status` directly instead of axios throwing on non-2xx, keeping the calling code's shape close to the previous `fetch`-based logic). Replaced `src/lib/authenticated-fetch.ts` with `src/lib/authenticated-request.ts` (same 401-refresh-and-retry behavior, now via `apiClient.request`). Converted all three client call sites — the login page, `LogoutButton`, and `ProductEditorForm` — from `fetch` to `apiClient`/`authenticatedRequest`.
- **My contribution:** Specified axios as a hard requirement, called out that it applies to _all_ client-side requests (not just the one being discussed), and asked for the retrofit rather than leaving the earlier `fetch`-based code in place.
- **Verification:** `npx tsc --noEmit`/`npm run lint`/`npm run build` clean. Re-ran the exact same manual tests as the previous entry, this time against the axios code: wrong-password login shows the error banner via `apiClient`; and the refresh-retry flow was re-verified end-to-end with the same shortened-TTL technique — network log showed `PATCH → 401` → `POST /api/admin/auth/refresh → 200` → `PATCH → 200` (retried) using axios, "Saved." shown with no visible error, and the new value confirmed persisted via `psql`. TTL and test data reverted afterward.
- **Reference:** [src/lib/api-client.ts](src/lib/api-client.ts), [src/lib/authenticated-request.ts](src/lib/authenticated-request.ts) (supersedes the now-deleted `src/lib/authenticated-fetch.ts` referenced in the previous entry)

### 2026-09-17 — Phase 4 continued: public catalog and product pages

- **Task:** With the admin side complete, implement the public-facing side: a catalog of published products and a per-product page with SEO meta tags, replacing the Phase 1 placeholders. Backend support (`listPublished`/`getPublishedBySlug`, draft-invisibility) already existed from Phase 2, so this was UI-only.
- **AI contribution:** Merged PR #3 (`admin-ui` → `develop`) first so the new `public-catalog` branch (per Git Flow) would include the font fix, shadcn components, and axios client instead of branching from a stale `develop`. Implemented `src/app/page.tsx` (catalog, reusing the admin list's card/row layout but without a status column since only published products are ever listed) and `src/app/products/[slug]/page.tsx` (name, characteristics as a definition list, description, `generateMetadata` sourcing `<title>`/meta description from the product's `seoTitle`/`seoDescription`). Added `loading.tsx` for both routes and a `not-found.tsx` for the product page. Both pages are `force-dynamic` for the same reason as the admin list (status/content can change at any time). Used React's `cache()` to dedupe the `getPublishedBySlug` call between `generateMetadata` and the page component instead of querying twice.
- **My contribution:** Directed the phase transition and confirmed the branch-basing question (merge PR #3 into develop first, rather than branching off `admin-ui` directly) to keep to the documented Git Flow.
- **Verification:** `npx tsc --noEmit`/`npm run lint`/`npm run build` clean (both routes correctly reported as dynamic `ƒ`, not prerendered). Manually verified in a live `next dev` session against the real seeded DB: catalog lists only the 2 published products (`wireless-mouse`, `usb-c-hub`), the draft `mechanical-keyboard` is absent from the list and 404s via the custom not-found page when visited directly by slug; product page renders characteristics/description correctly; confirmed via `document.querySelector('meta[name="description"]')` that the rendered meta description matches the product's `seoDescription`; checked both pages at mobile viewport width.
- **Reference:** [src/app/page.tsx](src/app/page.tsx), [src/app/products/[slug]/page.tsx](src/app/products/[slug]/page.tsx), [src/components/public/site-header.tsx](src/components/public/site-header.tsx)

### 2026-09-17 — Fix: `/admin/login` reachable while already authenticated

- **Task:** The user found that visiting `/admin/login` with a valid session still showed the login form instead of redirecting to the product list.
- **AI contribution:** The proxy's route matcher excluded `/admin/login` entirely (`/admin/((?!login).*)`), so it never ran the auth check for that route in either direction. Changed the matcher to cover `/admin/:path*` and added branching in `proxy()`: on `/admin/login`, an authenticated request is redirected to `/admin/products` and an unauthenticated one passes through; every other `/admin/*` route keeps the existing behavior (redirect to login when unauthenticated).
- **My contribution:** Reported the bug found through manual use of the running app.
- **Verification:** `npx tsc --noEmit`/`npm run lint`/`npm run build` clean. Verified live: with a valid session, navigating to `/admin/login` now redirects to `/admin/products`; after logging out, `/admin/login` renders normally.
- **Reference:** [src/proxy.ts](src/proxy.ts)

### 2026-09-17 — Phase 5: automated tests (unit + testcontainers integration)

- **Task:** Add the mandatory automated test suite per AGENTS.md/DEVELOPMENT-PLAN.md: unit tests for core business logic without a DB, and integration tests against a real ephemeral Postgres via `testcontainers`, covering save validation, draft invisibility, and auth gating.
- **AI contribution:**
  - Unit tests (`tests/unit/`, no DB): Zod schema limits/required fields (`validation.test.ts`), JWT sign/verify round-trips and tamper/wrong-secret rejection (`tokens.test.ts`), bcrypt hashing (`passwords.test.ts`), and `ProductService`/`AuthService` business logic (`product-service.test.ts`, `auth-service.test.ts`) exercised entirely with hand-written fakes satisfying the repositories' structural type — no mocking framework needed, confirming the repository/service DI split in AGENTS.md actually pays off for testability.
  - Integration tests (`tests/integration/`): a `testcontainers`-managed Postgres container per test run, migrated via `prisma migrate deploy` in `globalSetup`, with the connection string handed to test files through a temp file (`setup-env.ts`) since Jest's `globalSetup` runs outside the test workers' `process.env`. Tests import the real Route Handlers (`login`/`refresh`/`logout`, admin products list/detail/PATCH, public products list/detail) and invoke them directly with a hand-built `NextRequest` — no HTTP server needed. Covers: draft invisible via public API (list + direct slug, 404 same as unknown), auth gating (401 with no/garbage/expired token, 200 with valid), save validation (oversized description / empty required field rejected with 400 **and** the stored row verified unchanged via Prisma), and refresh-token rotation with replay protection (a used-and-rotated refresh token is rejected on reuse).
  - **⭐ Notable decision — `requireAdminId()` refactored to take `NextRequest` instead of `next/headers`' `cookies()`.** The original implementation (`await cookies()`) only works inside Next's own request-handling machinery, so the admin Route Handlers couldn't be invoked directly in a test. Changed its signature to `requireAdminId(request: NextRequest)` reading `request.cookies` — a Route Handler already receives its `NextRequest`, so this is a more idiomatic fit for that layer and makes the function a plain, directly-testable function of its input. Updated the two call sites (`GET /api/admin/products`, `GET`/`PATCH /api/admin/products/[id]`) accordingly. No pages use `requireAdminId` (page-level auth is fully handled by `src/proxy.ts`), so nothing else was affected.
  - **⭐ Notable decision — Jest runs under native ESM (`--experimental-vm-modules`), not the default CJS + ts-jest setup.** Discovered while writing the first integration test: Prisma 7's generated client (`src/generated/prisma/client.ts`) uses `import.meta.url` to locate its wasm query compiler, and `jose` (JWT signing) ships ESM-only with no CJS build — both throw "Must use import to load ES Module" under Jest's default CommonJS transform, and neither has a workaround short of avoiding the import entirely (not possible: the integration tests need the real Prisma client, and `tokens.ts` needs `jose`). Configured both Jest projects with `ts-jest/presets/default-esm` + `extensionsToTreatAsEsm` and added `NODE_OPTIONS=--experimental-vm-modules` to the `test:unit`/`test:integration` npm scripts instead. Also required `import { jest } from "@jest/globals"` in test files using `jest.fn()`, since Jest's automatic global injection doesn't extend to real ES modules.
  - Downgraded `testcontainers`/`@testcontainers/postgresql` from the latest `^12` to `11.14.0` after `npm install` warned it requires Node `>=22.22`, which is newer than this project's pinned `.nvmrc` (`22.21.1`); `11.14.0` has no engine constraint and installs cleanly.
- **My contribution:** Requested Phase 5 execution per the existing plan; no scope changes.
- **Verification:** `npx tsc --noEmit`/`npm run lint`/`npm run build` all clean. `npm run test:unit` — 39 tests passing, no DB/Docker involved. `npm run test:integration` — 16 tests passing against a real disposable Postgres container (migrations applied fresh each run, verified in the test output). One integration assertion (`toBeInstanceOf(Array)` on a JSON response body) initially failed due to a cross-realm `Array` constructor mismatch between the test file and Next's `Response`/JSON implementation — fixed by asserting `Array.isArray(...)` instead. After the `requireAdminId` refactor, re-verified the real admin flow live in `next dev` (not just the new tests): logged in, listed products, opened the editor, and saved a change — confirmed `PATCH /api/admin/products/[id] → 200` via `read_network_requests`, so the refactor didn't regress the actual running app.
- **Reference:** [jest.config.ts](jest.config.ts), [tests/unit/](tests/unit/), [tests/integration/](tests/integration/), [src/server/auth/guard.ts](src/server/auth/guard.ts)

### 2026-09-17 — Fix: `npm test` failing on a clean checkout (missing `ts-node`)

- **Task:** The user ran `npm test` on their machine right after Phase 5 and got `Error: 'ts-node' is required for the TypeScript configuration files`. Jest's own config loader needs `ts-node` to parse a `.ts` config file, and this session's shell happened to already have it available globally/cached, so the gap wasn't caught before committing `jest.config.ts`.
- **AI contribution:** Converted `jest.config.ts` to plain `jest.config.js` (CommonJS, `module.exports`), same content minus TS types — avoids adding `ts-node` as a dependency just to load the config.
- **My contribution:** Reported the exact failing command and stack trace from a real, unmodified checkout.
- **Verification:** Confirmed `node_modules/ts-node` doesn't exist in this project (matching the user's failure). Re-ran `npm run test:unit` (39 passing) and `npm run test:integration` (16 passing, real testcontainers Postgres) with the new JS config and no `ts-node` installed. `npx tsc --noEmit`/`npm run lint` clean.
- **Reference:** [jest.config.js](jest.config.js) (replaces the deleted `jest.config.ts`)

### 2026-09-17 — Fix: `npm test` crashing via a broken local `watchman`

- **Task:** After the previous fix, the user's `npm test` failed again — this time both `test:unit` and `test:integration` crashed with a `dyld` symbol-not-found error from their machine's Homebrew `watchman` binary (`fbthrift`/`watchman` version mismatch), which Jest shells out to by default to speed up file crawling.
- **AI contribution:** Set `watchman: false` in `jest.config.js`. Watchman only helps `--watch` mode's incremental re-runs; a one-shot `npm test`/CI run gets no benefit from it and Jest's built-in file crawler works without it, so disabling it entirely sidesteps the user's broken local install rather than asking them to fix their Homebrew setup.
- **My contribution:** Reported the exact crash output from their machine.
- **Verification:** Ran `npm run test:unit` and `npm run test:integration` without the `--no-watchman` flag I'd been passing manually in this session — both pass (39 and 16 tests respectively) with no watchman invocation at all.
- **Reference:** [jest.config.js](jest.config.js)

### 2026-09-17 — Strategic decision + implementation: frontend tests and Cypress e2e added to Phase 5

- **Task:** The user pointed out that Phase 5's automated tests only covered the backend, and asked for frontend unit/integration tests plus a Cypress e2e suite covering the core admin and public flows, and for AGENTS.md/DEVELOPMENT-PLAN.md's Phase 5 to be updated to reflect the expanded scope.
- **AI contribution:**
  - Recorded the decision first: added Cypress + React Testing Library/`jest-environment-jsdom` to AGENTS.md's tech stack table, and rewrote AGENTS.md/DEVELOPMENT-PLAN.md's Phase 5 sections to describe all four test layers before implementing them.
  - Added two more Jest projects, `frontend-unit` and `frontend-integration` (`jsdom`, plain CJS transform — frontend code never imports Prisma/`jose`, so the ESM workaround the backend projects need doesn't apply here). `tests/frontend-unit/` covers `StatusBadge` and `ProductEditorForm`'s client-side validation (Save button disabling) with no HTTP mocking at all; `tests/frontend-integration/` mocks `@/lib/api-client`/`@/lib/authenticated-request`/`next/navigation` with `jest.mock` to drive the login form, the editor's full save flow (success/rejected/session-expired/network-error), and the logout button end-to-end at the component level.
  - Added Cypress (`cypress.config.ts`, `cypress/e2e/*.cy.ts`, `cypress/support/commands.ts`'s `loginAsAdmin()`), wired to `npm run test:e2e` via `start-server-and-test` (boots `next dev`, waits for it, runs `cypress run`, tears down) with a `pretest:e2e` reseed step.
  - **⭐ Notable decision — found and fixed a real bug via the frontend integration test, not by inspection.** `LogoutButton`'s `try { await apiClient.post(...) } finally { ... }` had no `catch`, so a rejected logout request became an unhandled promise rejection (navigation to `/admin/login` still happened via `finally`, but the rejection was never handled). A test asserting "still navigates to login even if the logout request fails" surfaced this as a failing test (Jest treats the unhandled rejection as a test failure) before I'd even written an assertion about it. Fixed with a `catch { /* ignore, we navigate away unconditionally anyway */ }`.
  - **⭐ Notable decision — found and fixed a real bug via Cypress e2e, one the backend integration tests structurally couldn't catch.** `GET /products/mechanical-keyboard` (a draft) rendered the correct "not found" UI but with HTTP status **200**, not 404 — confirmed with `curl -D -` and by checking a genuinely nonexistent slug too (also 200), while `GET /api/products/mechanical-keyboard` and an entirely unmatched route both correctly returned 404. Root cause: Next's App Router can begin streaming a dynamic page's response before the page's own async data fetch resolves and calls `notFound()`, and the HTTP status can't change once streaming has started — a documented Next.js trade-off, not a bug in the app's logic, and structurally invisible to `tests/integration/`'s tests because those invoke Route Handlers directly (no live server, no streaming). Fixed by moving the check to `src/proxy.ts`: it now matches `/products/:slug`, does a `fetch` to the already-correct `GET /api/products/[slug]` Route Handler, and on a 404 there, rewrites to a path matching no route at all — reaching Next's own built-in 404 handling (verified separately to always return a real 404) before the page component ever renders. Added a root-level `src/app/not-found.tsx` (replacing the route-scoped one) so this is also the UI for the rewrite target. (An earlier attempt setting `htmlLimitedBots: /.*/` in `next.config.ts` to disable metadata streaming did **not** fix it — reverted — since the true cause was the page body's own streaming, not metadata specifically; left out of the final fix to avoid an unexplained config change.)
  - Excluded `cypress/` from the root `tsconfig.json` (`cypress/tsconfig.json` type-checks it separately) — Cypress's ambient Chai/Mocha types (`describe`, `expect`, etc.) collided with Jest's own globals when both were in the same TypeScript program, breaking `npx tsc --noEmit` for every test file in the repo.
- **My contribution:** Identified the backend-only gap, specified frontend unit/integration + Cypress e2e covering "all core user flows for both admin/public side", and asked for the plan docs to be updated as part of the same change.
- **Verification:** `npx tsc --noEmit` (both the root and `cypress/tsconfig.json`), `npm run lint`, and `npm run build` all clean. `npm test` — 39 + 16 + 9 + 9 = 73 Jest tests passing (backend unit/integration unchanged, 18 new frontend tests). `npm run test:e2e` — all 14 Cypress tests passing on a real `next dev` server against the Docker Compose Postgres, confirmed via server request logs that the proxy's `/api/products/[slug]` check and rewrite actually fire (`GET /api/products/mechanical-keyboard 404` → `GET /products/mechanical-keyboard 404`), and confirmed via a direct Prisma query after the run that `publish-status.cy.ts` left the database exactly as seeded (`mechanical-keyboard` back to `DRAFT`, `usb-c-hub`'s description restored).
- **Reference:** [tests/frontend-unit/](tests/frontend-unit/), [tests/frontend-integration/](tests/frontend-integration/), [cypress/](cypress/), [src/proxy.ts](src/proxy.ts), [src/app/not-found.tsx](src/app/not-found.tsx), [src/components/admin/logout-button.tsx](src/components/admin/logout-button.tsx)

### 2026-09-17 — Fix: cryptic crash on `npm run test:e2e` under the wrong Node version

- **Task:** The user ran `npm run dev` then `npm run test:e2e` and got a raw `Error [ERR_REQUIRE_ESM]` stack trace from deep inside `prisma db seed`'s internals (`@prisma/dev`'s `zeptomatch` dependency), ending in `Node.js v20.14.0` — the same Node 20.14 vs. the project's pinned 22.21.1 mismatch from earlier in the project (the arborist crash during initial `npm install`), just resurfacing in a new terminal session that hadn't run `nvm use`.
- **AI contribution:** `require(esm)` only works on Node ≥22.12/20.19, so this specific error is what you get on 20.14 with no clear indication of the real cause. Rather than only re-explaining "run `nvm use`" again, added a guard: `package.json` now declares `"engines"` and a new `.npmrc` sets `engine-strict=true` (helps at `npm install` time), plus a small dependency-free `scripts/check-node-version.cjs` wired as a `pre*` hook on `dev`, `build`, `start`, `seed`, and `test` (covering `test:e2e` transitively via its `pretest:e2e` → `seed` chain) — so running any of these on an unsupported Node version now fails immediately with "This project needs Node ... — currently running Node 20.14.0. Run "nvm use"..." instead of a multi-hundred-line minified stack trace.
- **My contribution:** Reported the exact failing command sequence and full error output.
- **Verification:** Manually switched to Node 20.14.0 and confirmed `npm run seed` now fails fast with the clear guard message instead of the original stack trace; switched back to 22.21.1 (`nvm use`) and confirmed `check-node` passes silently and `npm run seed`, `npx tsc --noEmit`, `npm run lint`, `npm run build`, and `npm run test:unit` all still work normally.
- **Reference:** [scripts/check-node-version.cjs](scripts/check-node-version.cjs), [package.json](package.json), [.npmrc](.npmrc)

### 2026-09-17 — Temporarily disabled the Node-version pre-hooks

- **Task:** The user asked to temporarily disable the Node-version check just added to `dev`/`build`/`start`/`seed`/`test`.
- **AI contribution:** Removed the `pre*` hook wiring (`predev`, `prebuild`, `prestart`, `preseed`, `pretest`) from `package.json` so those scripts run unchecked again, without deleting the underlying guard: `scripts/check-node-version.cjs`, the `check-node` script itself (still runnable manually), the `engines` field, and `.npmrc`'s `engine-strict=true` are all left in place so the check can be re-wired later by re-adding the `pre*` lines.
- **My contribution:** Requested the temporary disable, presumably to unblock work on the wrong Node version without fighting the guard.
- **Verification:** `package.json` still valid JSON; `npm run` script list confirmed to no longer include the `pre*` hooks.
- **Reference:** [package.json](package.json)

### 2026-09-17 — Re-scoped the Node-version guard to `seed` only

- **Task:** After explaining the `ERR_REQUIRE_ESM` root cause (Node's `require(esm)` support, needed only by `prisma db seed`'s `@prisma/dev` dependency, not by the app itself) and comparing the version-checker approach against baking `nvm use` into scripts (rejected: `nvm` is a shell function sourced from `.bashrc`/`.zshrc`, not visible to the plain `sh -c` subshell npm scripts run in, and it's nvm-specific), the user pointed out that the previously-disabled guard, if re-enabled as it was, would have blocked `dev`/`build`/`start`/`test` too — even though their own `npm run dev` had already run fine on Node 20.14.
- **AI contribution:** Re-added the `check-node` pre-hook to `seed` only (`preseed`), not `dev`/`build`/`start`/`test` — the only npm script confirmed to actually touch the Node-version-sensitive code path. Updated the guard script's comment to state this scoping explicitly, so a future edit doesn't casually re-broaden it.
- **My contribution:** Caught that the original guard was scoped too broadly by testing `npm run dev` themselves and noticing it worked despite the "unsupported" Node version.
- **Verification:** Confirmed on Node 20.14.0: `npm run seed` still fails fast with the clear guard message; `npm run dev` starts normally and serves the app (`✓ Ready`, `GET /` reachable) with no guard interference. Switched back to Node 22.21.1 and confirmed `npm run seed` still works normally there too.
- **Reference:** [package.json](package.json), [scripts/check-node-version.cjs](scripts/check-node-version.cjs)

### 2026-09-17 — Phase 6: docs pass

- **Task:** Finalize README.md (setup/run/test instructions, test admin credentials, architecture notes, known limitations, actual time spent) and bring AI-WORKLOG.md up to date, per DEVELOPMENT-PLAN.md's Phase 6.
- **AI contribution:** Filled in README's "Time spent" TODO with an estimate derived from `git log --all` commit timestamps (~9.5h, slightly over the 6–8h core budget, mainly attributable to Phase 5's scope growing mid-project). Fixed a stale claim in the Prerequisites section (said `dev`/`build`/`start`/`seed`/`test` all check the Node version, which was true right after that guard was first added but became inaccurate once it was rescoped to `seed` only). Updated the top status banner to reflect Phases 1–6 complete. Removed a leftover "(fill in others as used...)" placeholder row from the AI-WORKLOG tools table (no other AI tools were actually used) and added a dedicated "Role of automated tests in verifying AI-generated code" section summarizing how tests were verified against real infrastructure, the two real bugs the test suites themselves caught, and how the AI-written test assertions were quality-checked (asserting persisted state, not just response shape).
- **My contribution:** Directed the phase transition; no scope changes.
- **Verification:** Read through README.md and AI-WORKLOG.md end to end for staleness (`grep -rn TODO` across all doc files came back empty afterward) rather than only appending new content.
- **Reference:** [README.md](README.md), [AI-WORKLOG.md](AI-WORKLOG.md)

### 2026-09-17 — Correction: "Time spent" replaced with real Clockify tracking

- **Task:** The previous entry's "Time spent" figure (~9.5h, derived from `git log --all` commit timestamps) overcounted real working time, since it measured wall-clock span between the first and last commit rather than actual time spent — it doesn't know about breaks. The user tracked time attentively in Clockify throughout and provided the exported PDF summary report to replace it with real numbers.
- **AI contribution:** Read the Clockify PDF (`Clockify_Time_Report_Summary_14_09_2026-20_09_2026.pdf`, total 08:41:47 through Phase 5) and replaced README's "Time spent" table with the real per-entry durations, mapped to phases. Where a single Clockify entry names multiple activities (e.g. "Execute Phase 4 (UI implementation) + Fix missing-slug + Connect token refresh endpoint + Add Axios + Planning Phase 5 + Execute Phase 5 (Audo tests)", 1h39m45s), its duration was split evenly across the number of named activities rather than guessed at — the export doesn't sub-divide a single tracked entry any further, so an even split is the only defensible allocation without more granular source data. Verified the resulting per-phase numbers sum back to the report's own total (08:41:47) before writing them into README.
- **My contribution:** Caught the estimation error and supplied the ground-truth source (the Clockify report) instead of just asking for a bigger guess.
- **Verification:** Summed all extracted per-phase durations in seconds and confirmed the total matches the PDF's reported "Total: 08:41:47" exactly.
- **Reference:** [README.md](README.md) (Time spent section)

### 2026-09-17 — Correction: fixed two wrong dates in this log's own entry headers

- **Task:** The user noticed the Sep 16/Sep 17 date headers in this file looked mixed up and asked for a review, then supplied their own memory of the timeline (planning started 09-14/09-15 and finished 09-16; Phase 1 and Phase 2 also finished 09-16; everything else is 09-17) to verify against the Clockify report.
- **AI contribution:** Cross-checked every entry header two ways: against the corresponding commit's actual git timestamp, and against the Clockify PDF's per-day totals (Sep14: 00:25:55, Sep15: 00:38:45, Sep16: 03:38:46, Sep17: 03:58:21). The user's memory reconciled exactly: Sep14+Sep15 (01:04:40) matches the "Planning" entries' portion that predates any commit; Sep16 (03:38:46) matches the remaining planning time plus all of Phase 1 and Phase 2's logged durations to the second; Sep17 (03:58:21) matches the sum of every remaining entry. Found two mismatches: the "Planning: stack, architecture, and development order" entry was dated as a single day (2026-09-16) when it actually spans 2026-09-14 to 2026-09-16, and the "`/admin/login` reachable while already authenticated" fix was dated 2026-09-16 when its commit landed at 02:51 on 2026-09-17. Fixed both headers; found no other mismatches.
- **My contribution:** Flagged the date inconsistency, held off approving any commit until asked to verify against a second independent source (git timestamps had already been checked in the prior turn), then supplied the specific day-by-day breakdown from memory for cross-verification.
- **Verification:** Summed the relevant Clockify entry durations against each day's reported total and confirmed exact matches (to the second) for all three days before editing anything.
- **Reference:** [AI-WORKLOG.md](AI-WORKLOG.md)

### 2026-09-17 — Correction: Phase 3 (design) time moved from core to bonus budget

- **Task:** After a full requirements-compliance review, the user pointed out that the "Time spent" section's core total (8h41m47s) wrongly included Phase 3 (Figma design, ~1h00m43s) — Design Tools is explicitly a bonus item in PROJECT-REQUIREMENTS.md, and AGENTS.md's own time-budget policy says bonus work isn't counted against the 6–8h core budget.
- **AI contribution:** Split README's "Time spent" section into a "Core budget" table (Planning + Phases 1/2/4/5, excluding design) totaling **7h41m04s** — which now fits inside the stated 6–8h core budget — and a separate "Bonus budget" table showing Phase 3's 1h00m43s against the ~2–3h bonus allotment. Cross-referenced the same figure into the Bonus features table's Design Tools row.
- **My contribution:** Caught the miscategorization during a full requirements-compliance review requested separately.
- **Verification:** Re-summed the core-only phases (2:58:50 + 0:53:43 + 0:33:55 + 1:17:54 + 1:56:42 = 7:41:04) and confirmed it plus Phase 3's 1:00:43 reproduces the original Clockify-verified total of 8:41:47.
- **Reference:** [README.md](README.md) (Time spent section)

### 2026-09-17 — Bonus: Infrastructure (Docker Compose full-stack + CI)

- **Task:** Implement the Infrastructure bonus per PROJECT-REQUIREMENTS.md/DEVELOPMENT-PLAN.md's Phase 7: run the whole app via Docker Compose or CI with lint/tests/build. AGENTS.md commits to both.
- **AI contribution:**
  - Added `output: "standalone"` to `next.config.ts` and a multi-stage `Dockerfile` (`deps` → `builder` → `runner`), so the runtime image only ships the standalone bundle, not `node_modules`/source.
  - Extended `docker-compose.yml` with two new services: `migrate` (a one-off job on the `builder` stage running `prisma migrate deploy && npm run seed`, so `docker compose up` always starts from a migrated, seeded database) and `web` (the actual app, `depends_on: migrate: condition: service_completed_successfully`). `DATABASE_URL` is hardcoded to the `postgres` service's Compose network hostname for both, since `.env`'s `DATABASE_URL` is set up for connecting from the host machine during normal un-dockerized `npm run dev` and would be wrong inside the Compose network; JWT secrets still come from `.env` via `env_file`.
  - Added `.github/workflows/ci.yml`: four jobs (lint/typecheck/build, backend tests, frontend tests, e2e tests with a Postgres service container) running on every push and PR, each independently so a failure is easy to pinpoint. The e2e job reuses the exact same `npm run test:e2e` script used locally, pointed at the service container instead of the Docker Compose Postgres.
  - Two real CI-only failures surfaced by actually pushing and watching the workflow run, not by writing it and assuming it would pass: `tsc --noEmit` failed on a fresh checkout with `Cannot find name 'LayoutProps'`, because that ambient type is generated by Next into `.next/types` on first `next dev`/`next build` — never a problem locally since `.next` already existed from prior runs. Fixed with `next typegen` (generates just the ambient route types, no full build) as a step before `tsc`. Separately, the e2e job flaked on `cy.location()` timing out right after a first-time navigation to a product page, because `next dev` compiles each route on first visit and the CI runner was slow enough to exceed Cypress's 4s default command timeout; raised `defaultCommandTimeout` to 15s in `cypress.config.ts`.
- **My contribution:** Directed the phase transition to the Infrastructure bonus item specifically (of the three listed).
- **Verification:** `docker compose up --build` — confirmed via container logs that `migrate` applied migrations and seeded successfully before `web` started; smoke-tested the running containerized app with `curl` (catalog, published/draft product pages — including the proxy-based 404 fix — and a full `/api/admin/auth/login` round trip returning valid cookies) and in a real browser (full login → product list flow, including the `Secure` cookie flag under `NODE_ENV=production`, which browsers permit over plain HTTP specifically for `localhost`). Pushed the CI workflow and used `gh pr checks --watch` to confirm all four jobs actually pass on GitHub Actions (not just assumed from local runs) before merging — this is what caught the two CI-only failures above.
- **Reference:** [Dockerfile](Dockerfile), [docker-compose.yml](docker-compose.yml), [.github/workflows/ci.yml](.github/workflows/ci.yml), [next.config.ts](next.config.ts), [cypress.config.ts](cypress.config.ts)

### 2026-09-18 — Bonus: LLM integration (OpenAI, mock mode default)

- **Task:** Implement the LLM integration bonus per PROJECT-REQUIREMENTS.md: generate description/SEO fields via a real LLM API (provider of choice), Ukrainian output based on name+characteristics respecting the editor's limits, preview/apply/discard without auto-saving, no data loss or UI hang on integration errors, and reproducibly testable without a reviewer's API key (mock mode marked explicitly).
- **AI contribution:**
  - **⭐ Notable decision — provider abstraction mirrors the existing repository/service DI pattern.** Added `src/server/services/llm/`: an `LlmProvider` interface (`types.ts`), two implementations (`MockLlmProvider`, deterministic offline Ukrainian generator; `OpenAiProvider`, calls the real Chat Completions API with a Ukrainian system prompt and JSON response format), and `SuggestionService` which picks one based on `OPENAI_API_KEY`'s presence and validates/sanitizes the result — same shape as `ProductRepository`/`AuthService`'s injectable dependencies, chosen specifically so both providers are unit-testable with a fake and the route handler never needs to know which is active.
  - **⭐ Notable decision — sanitize by truncating, but reject on empty, not the other way round.** A real LLM's output is unpredictable; `sanitize()` in `suggestion-service.ts` trims and truncates an over-length field to fit (still useful to the user) but throws `InvalidSuggestionError` on an empty one (no truncation fixes that, and showing an empty "suggestion" would be worse than a plain error) — directly satisfies the requirement that unsuitable responses not cause data loss or a stuck UI.
  - Extracted `productContentFieldsSchema` (description/seoTitle/seoDescription only) out of `productEditableFieldsSchema` in `src/lib/validation/product.ts` so the same limits/non-empty rules govern a manual save and a generated suggestion, without duplicating them.
  - Added `POST /api/admin/products/[id]/suggest` (auth-gated, read-only — never writes to the DB) and a "Suggest with AI" button + preview panel in `ProductEditorForm`, with its own independent pending/error state so a suggestion failure can never be confused with a save result. Applying a suggestion only updates the form's local React state (still requires the existing, separate Save action to persist); the preview is clearly labeled "Simulated (no API key set)" when mocked.
- **My contribution:** Directed the phase transition to the LLM bonus item; reviewed and manually verified the mock-mode UX in a live browser session before writing automated tests around it.
- **Verification:** Manually verified the whole flow in a live `next dev` session: generated a mock suggestion for USB-C Hub 7-in-1, confirmed the "Simulated" badge and Ukrainian content within limits, applied it (fields updated, no network write), then reloaded the page and confirmed the original saved values were untouched (proving apply-without-save). Added `tests/unit/mock-llm-provider.test.ts` and `tests/unit/suggestion-service.test.ts` (limits/truncation/empty-rejection/error-propagation with a fake provider), `tests/integration/admin-suggest.test.ts` (auth gating, 404, mocked response shape, and that generation never modifies the stored row — against a real testcontainers Postgres), `tests/frontend-integration/product-editor-form-suggest-flow.test.tsx` (preview/apply/discard/error/network-error/401 flows with the HTTP layer mocked), and a Cypress e2e test in `admin.cy.ts` covering the same golden path against a real running app. `npx tsc --noEmit`, `npm run lint`, `npm run build` clean; full suite passing (91 Jest tests, 15 Cypress tests). Real (OpenAI) mode was implemented against OpenAI's documented API contract and covered by the same server-side validation as mock mode, but **not exercised against the live API** — no `OPENAI_API_KEY` was available in this environment; disclosed as a known limitation in README rather than claimed as verified.
- **Reference:** [src/server/services/llm/](src/server/services/llm/), [src/app/api/admin/products/[id]/suggest/route.ts](src/app/api/admin/products/[id]/suggest/route.ts), [src/components/admin/product-editor-form.tsx](src/components/admin/product-editor-form.tsx)

### 2026-09-18 — Bonus: Shopify import

- **Task:** Implement the Shopify import bonus per PROJECT-REQUIREMENTS.md: import a product from a test Shopify store via the Admin API, save it into this app's own database, and show it in the editor.
- **AI contribution:**
  - A Shopify MCP connection became available in this session, tied to the user's own trial-plan Shopify store ("My Store", `id111w-aj.myshopify.com`) — confirmed read-only via `get-shop-info` before doing anything else. The store had zero products, so real test data was needed to build and verify the import against; asked the user how they wanted a product added rather than writing to their (real, if trial) Shopify account unprompted. They asked for two sample products to be added directly.
  - Created two draft products in that store via the connected Shopify tools ("Bluetooth Wireless Earbuds", "Stainless Steel Water Bottle" — realistic titles/vendors/options/HTML descriptions, deliberately including HTML markup in one to exercise description sanitization) and fetched their real data back via `get-product`/`graphql_query` to learn the exact Admin API response shape (handle, options as `{name, values}`, HTML `body_html`, etc.) before writing any mapping code — building against confirmed real data rather than guessing at Shopify's schema.
  - **⭐ Notable decision — no mock mode for this bonus, unlike the LLM one.** PROJECT-REQUIREMENTS.md's LLM section explicitly requires reproducible testing without a reviewer's API key; its Shopify section doesn't say this. Kept `ShopifyClient` as an injectable interface (same DI shape as `LlmProvider`) for testability, but the app itself has no "no credentials" fallback — `ShopifyImportService` throws `ShopifyNotConfiguredError` (503, clear message, no crash) when `SHOPIFY_STORE_DOMAIN`/`SHOPIFY_ADMIN_API_TOKEN` are unset, verified live in the browser.
  - **⭐ Notable decision — real fetched data used as the test fixture, not invented data.** `tests/unit/shopify-mapper.test.ts` and `tests/integration/admin-import.test.ts` use the *actual* JSON fetched back from the live "Bluetooth Wireless Earbuds" product (including its real HTML body) as their fixture, so the HTML-stripping/mapping logic is verified against genuine Shopify output shape, not a hand-typed guess at what Shopify might return.
  - Added `findBySlug`/`create` to `ProductRepository` (the only place the admin API creates a product — an explicit, scoped exception to "the editor never creates/deletes," see `AGENTS.md`), `src/server/services/shopify/` (types/client/mapper/import-service, mirroring the LLM feature's structure), `POST /api/admin/products/import`, and a `ShopifyImportForm` on the admin product list that redirects straight to the new (draft) product's editor on success.
- **My contribution:** Directed the phase transition to the Shopify bonus; decided to add sample products directly rather than have the user do it manually, once asked.
- **Verification:** Manually verified live in a real browser session: logged in, saw the import form, submitted the real product ID (`8325302452342`), confirmed the exact "not configured" error rendered cleanly with no crash and the rest of the product list unaffected (network log confirmed `POST .../import → 503`) — this is the actual state of the app in this environment (no Admin API token available; creating one requires a manual step in the Shopify admin's "custom app" settings that nothing here can automate). What *was* fully verified: `tests/unit/shopify-mapper.test.ts` (7 tests) and `tests/unit/shopify-import-service.test.ts` (7 tests) against the fake client; `tests/integration/admin-import.test.ts` (5 tests) exercises the real create-in-database path (slug uniqueness included) against a real testcontainers Postgres, with only the Shopify network call faked; `tests/frontend-integration/shopify-import-form.test.tsx` (4 tests) covers the form's success/error/network-error/401 flows. `npx tsc --noEmit`, `npm run lint`, `npm run build` clean; full suite passing (114 Jest tests, 15 Cypress tests — no new e2e test added here since it would need real credentials this environment doesn't have).
- **Reference:** [src/server/services/shopify/](src/server/services/shopify/), [src/app/api/admin/products/import/route.ts](src/app/api/admin/products/import/route.ts), [src/components/admin/shopify-import-form.tsx](src/components/admin/shopify-import-form.tsx), [tests/unit/shopify-mapper.test.ts](tests/unit/shopify-mapper.test.ts)

### 2026-09-18 — Shopify auth updated for the new Dev Dashboard flow; real import verified live

- **Task:** The user tried to get a Shopify Admin API token for the previous entry's implementation and found the documented "Settings → Apps → Develop apps → reveal static token" path no longer exists — Shopify deprecated it on 2026-01-01. Investigated current docs, updated the app's Shopify auth to match, and (once the user had real credentials) verified the whole import feature against a live network call for the first time.
- **AI contribution:**
  - Used live web search/fetch (not training-data memory, which predates this change) to confirm: new apps are created via the [Dev Dashboard](https://dev.shopify.com/dashboard/) and only expose a Client ID/Secret; getting an Admin API token now requires a client-credentials grant (`POST /admin/oauth/access_token` with `grant_type=client_credentials`), valid ~24h, refreshed ahead of expiry rather than per-request.
  - **⭐ Notable decision — added a `ShopifyTokenProvider` abstraction instead of hard-coding one auth method.** `StaticTokenProvider` (wraps a fixed token — the pre-2026 legacy path, still valid for existing apps) and `ClientCredentialsTokenProvider` (performs the grant, caches the token with a 60s expiry safety margin, re-fetches once stale) both implement the same interface; `ShopifyAdminApiClient` just calls `getAccessToken()` before each request and doesn't know which is active. `resolveShopifyClient()` prefers a static token if `SHOPIFY_ADMIN_API_TOKEN` is set, falling back to the client-credentials pair — so both a legacy app and a new Dev Dashboard app work without any code branching at the call site.
  - When the first live attempt failed with `Oauth error app_not_installed`, temporarily logged the token-exchange response body (safe — it's Shopify's own error text, not the secret) to diagnose it, found the real cause, fixed the actual setup (installing the app on the store, a separate step from creating it), then reverted the temporary logging rather than leaving debug-only behavior in.
- **My contribution:** Reported each error message and dashboard state verbatim as they occurred (the Client ID/Secret-only screen, then `app_not_installed`), rather than guessing at fixes; decided to implement the full client-credentials flow rather than just documenting a manual workaround; obtained the Dev Dashboard app and set the real credentials in their own `.env` (never shared in chat).
- **Verification:** `tests/unit/shopify-token-provider.test.ts` (6 tests, mocking `global.fetch`) covers the exchange request shape, caching, expiry-triggered re-fetch, and both failure modes. Then, with real credentials in `.env`, drove the actual admin UI in a live browser session and imported the real "Bluetooth Wireless Earbuds" product from the live test store end-to-end — confirmed the created draft's description (HTML correctly stripped to plain text), characteristics (Vendor/Type/Color), and SEO fields all matched the real Shopify data, via a genuine network round trip (server logs showed the real `POST .../import` succeeding, not a fake). This is the first time in this project a bonus feature's "real mode" was verified against a live external API rather than left as a disclosed limitation. `npx tsc --noEmit`/`npm run lint`/`npm run build` clean; full suite passing (120 Jest tests).
- **Reference:** [src/server/services/shopify/token-provider.ts](src/server/services/shopify/token-provider.ts), [src/server/services/shopify/shopify-client.ts](src/server/services/shopify/shopify-client.ts)

### 2026-09-18 — Migrate remaining server-side `fetch` calls to axios

- **Task:** The user asked to confirm whether browser-side HTTP already used axios everywhere (it did — a `grep` for `fetch(` across `src/` turned up zero matches under `*.tsx`), then asked to also replace the three remaining Node.js server-side `fetch` calls (OpenAI provider, Shopify Admin API client, Shopify token exchange) with axios, and to record this as a strategic decision in [AGENTS.md](AGENTS.md).
- **AI contribution:**
  - Added `src/server/lib/http-client.ts`, a shared axios instance for server-side (Node.js) calls to third-party APIs — mirroring the existing browser instance in `src/lib/api-client.ts`, including its `validateStatus: () => true` convention so callers inspect `response.status` directly instead of axios throwing on a non-2xx response.
  - Migrated `openai-provider.ts`, `shopify-client.ts`, and `token-provider.ts` to use it, typing the response as `AxiosResponse` (an `Awaited<ReturnType<typeof httpClient.post>>` alias resolved to `unknown` across all three files due to axios's overloaded generics, so the explicit import was used instead).
  - Explicitly kept `src/proxy.ts`'s `fetch` call untouched and called this out as a strategic exception in the AGENTS.md tech-stack table: it runs on the Edge runtime, which has no Node `http`/`net` modules for axios to use.
  - **⭐ Notable decision — DI over `jest.mock` to fix the token-provider test, not the other way round.** Migrating `token-provider.ts` broke `tests/unit/shopify-token-provider.test.ts`, which mocked `global.fetch` — axios uses Node's `http`/`https` modules directly, so the mock stopped intercepting anything. `jest.mock("@/server/lib/http-client", ...)` would have "fixed" this with the least code change, but no other unit test in this codebase uses `jest.mock` (everything else uses constructor-injected fakes — repositories, `LlmProvider`, `ShopifyClient`), and this project's Jest ESM setup (`ts-jest/presets/default-esm` + `--experimental-vm-modules`) has no precedent proving `jest.mock` works reliably here. Rather than introduce an unverified pattern to save a few lines, added an optional `http: AxiosInstance = httpClient` constructor parameter to `ClientCredentialsTokenProvider` (same shape as every other injectable dependency in the codebase) and rewrote the test to inject a fake `{ post: jest.fn(...) }` — consistent with the established testability principle in AGENTS.md rather than a one-off exception to it.
- **My contribution:** Asked the clarifying "fetch vs axios" question that surfaced this as unfinished migration debt from the original Phase 4 axios adoption; approved treating it as a strategic decision worth recording in AGENTS.md rather than a silent refactor.
- **Verification:** `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean (route list unchanged). Full Jest suite green: 67 unit + 25 integration (real testcontainers Postgres) + 28 frontend (9 unit + 19 integration) = 120 tests, including the rewritten `shopify-token-provider.test.ts` (6 tests: static token, client-credentials exchange request shape, caching, expiry-triggered re-fetch, both failure modes). Re-verified live against the real Shopify test store used in the prior entry by importing a second real product ("Stainless Steel Water Bottle", ID `8325302485110`) through a running `next dev` session — confirmed the axios-based client and token provider still work correctly end-to-end against the live Admin API (vendor/type/options/HTML-stripped description all matched). Cypress e2e suite was not re-run in this sub-task (no HTTP-client-visible behavior change at that layer); last verified green in the prior PR's CI run.
- **Reference:** [src/server/lib/http-client.ts](src/server/lib/http-client.ts), [src/server/services/llm/openai-provider.ts](src/server/services/llm/openai-provider.ts), [src/server/services/shopify/shopify-client.ts](src/server/services/shopify/shopify-client.ts), [src/server/services/shopify/token-provider.ts](src/server/services/shopify/token-provider.ts), [tests/unit/shopify-token-provider.test.ts](tests/unit/shopify-token-provider.test.ts)

### 2026-09-18 — Fix: admin header title and full-bleed width

- **Task:** The user pointed out `AdminTopBar` showed the current page's own title ("Products" / "Edit product") instead of the project title, and that its inner container was constrained to the same `max-w-2xl` as the page content below it, when the Figma design has it full-bleed.
- **AI contribution:** Attempted to re-check the actual Figma mockups first (`get_metadata` on the design file), but the Figma MCP connection had already hit its Starter-plan tool-call rate limit (previously logged for the mobile Product Editor screen — see the 2026-09-17 Design phase entry) and refused the call. Rather than guess blindly, inferred the fix from two things already in the codebase: (1) the page-level `<h1>` on both admin pages already repeats the exact text `AdminTopBar`'s `title` prop was passing it (`"Products"` / product name), making the top-bar title pure duplication; (2) the public `SiteHeader` component shows the fixed project name `"Product Content Studio"` in the identical layout position. Removed `AdminTopBar`'s `title` prop entirely, hardcoded it to `"Product Content Studio"` to match `SiteHeader`, and changed its inner wrapper from `mx-auto w-full max-w-2xl` to a plain `w-full` (full-bleed bar, only the content below stays centered) — mirroring the width relationship the user described from the design.
- **My contribution:** Flagged the visual mismatch and asked for a Figma cross-check before accepting a fix based on code inference alone.
- **Verification:** `npx tsc --noEmit` and `npm run lint` clean. Manually verified in a live `next dev` browser session, logged in as the seeded admin: both `/admin/products` and `/admin/products/[id]` now show a full-width "Product Content Studio" header (edge-to-edge, unlike the centered content below), checked at both desktop and 375px mobile widths. Confirmed `cypress/e2e/admin.cy.ts`'s existing `cy.contains("h1", "Products")` assertion still targets the page's own `<h1>`, not the top bar, so it isn't affected by the top-bar text change. **Caveat:** the Figma file itself could not be re-opened to visually confirm this matches the mockup pixel-for-pixel, since the MCP rate limit was still in effect — this fix is based on internal consistency (matching `SiteHeader`, removing duplicate titles) rather than a fresh screenshot comparison; worth a follow-up visual diff once the Figma MCP quota resets.
- **Reference:** [src/components/admin/admin-top-bar.tsx](src/components/admin/admin-top-bar.tsx), [src/components/public/site-header.tsx](src/components/public/site-header.tsx)

<!-- Further entries appended below as implementation proceeds. -->
